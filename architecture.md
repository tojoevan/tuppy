# Mate 个人 AI 层 — 架构设计与说明

> 版本：v1.0（2026-08-14）
> 定位：一个归用户所有的 agent，替用户看着自己的世界，只报与用户相关的事，判断权留给用户，且不从平台拿钱。

## 1. 产品定位

### 1.1 问题

- 现有 AI 产品全部是 tool 范式：用户发起 turn，agent 响应，任务完即消失。任务间隙 agent 是死的。
- 平台 AI 化加剧噪音：过去是 8 个哑面板，现在是 8 个会说话的 AI 各自主动推送，噪音不降反升。
- 用户被信息淹没，且所有平台 AI 的老板是平台——利益不对齐（engagement 是平台 KPI，不是用户的注意力）。
- 用户被"生物电池化"：免费工具 → 平台锁定 → 注意力/数据变资产，AI 加速后每次使用既是训练数据又是 token 营收。

### 1.2 解法：Mate 范式

产品边界从"完成任务"变成"维持关系"。与现有产品的五个区别：

| 维度 | 现有产品 | Mate |
|---|---|---|
| 注意力 | 消耗用户注意力（盯它下指令） | 管理用户注意力（过滤噪音，只浮出值得知道的） |
| 上下文 | 单域 | 跨域（代码 + 日程 + 家庭 + 收入） |
| 时间 | 会话制，结束即死 | 连续存在，用户睡了它还在观察 |
| 信任 | 不可见 | 信任资本公开可查（hit rate、履职记录） |
| 闲置 | 死 | 低风险自选工作 |

### 1.3 一句话定位

**其他工具告诉你世界上发生了什么，它只告诉你和你有关的事。**

### 1.4 楔子（切入点）

- 家庭数据场景（已有小程序）：账本异常、日程冲突、健康记录遗漏、物品临期。100% 私有数据，无平台覆盖，天然无竞争。
- 后续楔子：独立开发者私有流（CI、issue、监控、收入、依赖、竞品）。

## 2. 核心原则

### 2.1 权力三级宪法

| 级别 | 内容 | 权限 |
|---|---|---|
| 处理 | 合并、去重、延迟、归档、分类 | 可全权代理 |
| 判断 | 什么重要、阈值、排序、主动推送 | 规则用户可见可改，每次判断留痕 |
| 决定 | 回复、行动、花钱、对外 | 默认禁止，只能提议 |

处理可以全权，判断必须透明，决定必须请示。

### 2.2 权限分级（动作维度）

1. **观察**：随便做，零打扰（读文件、看日志、跟踪状态）
2. **提议**："我注意到 X，要不要处理？"——最安全档，mate 感 90% 来自这档
3. **可逆执行**：能回滚，先做后说（有 undo 日志）
4. **不可逆/对外**：发消息、部署、花钱——必须先问

### 2.3 打扰分级

| 级别 | 方式 | 通道 |
|---|---|---|
| 观察 | 静默，自己跟踪 | 无 |
| 摘要 | 攒进早晚报/履职回放 | 小程序 |
| 推送 | 值得现在知道 | 小程序/IM |
| 打断 | 紧急（挂机、截止） | Bark/高优先级通道，极罕用 |

- **每日打扰预算**：硬上限，超限自动降级为摘要。预算用尽 agent 闭嘴，除打断级外全部写进下次摘要。
- 反馈循环：用户忽略 → 降权；响应 → 加权；手动询问 → 下次主动推。三周后阈值贴合用户实际关注。

### 2.4 信任原则（恐惧解药）

科幻里 AI 失控几乎从来不是"模型太强"，而是人类把决定权让渡后拿不回来。所以：

1. **挡掉 ≠ 删掉**：一切被滤掉的信息进归档，可搜索可浏览。摘要每条带链接可展开看原始流。
2. **可审计**：履职记录公开——今天吸收多少条、浮出多少条、按什么规则滤的。规则可查看可修改。黑箱是恐惧温床，白箱才有信任。
3. **可逆转**：可逆动作带 undo 日志。
4. **可解雇**：一键静默、一键降级、一键导出全部数据离开。用户知道自己能随时解雇它，才敢真用它。

### 2.5 拥有权原则（生物电池解药）

逃逸条件只有一个：拥有权。租的 agent vs 有的 agent，区别不是功能，是解雇权。四个硬选择：

1. **本地优先**：过滤层跑本地小模型，日常数据不出设备
2. **模型无关**：用户自己的 key、自己的模型。不靠卖用户 token 赚钱
3. **数据是文件**：SQLite 导出即全部。反馈训练的过滤模型随数据走
4. **收软件钱，不收注意力钱**：商业模式即产品声明——订阅费买的就是"它没拿平台的钱"

三个测试："模型能换？数据能拿？agent 能杀？" 三个 yes 是雇主，任何一个 no 是电池。

### 2.6 交互原则

- **不编程它，养它**：人不擅长写 spec，擅长做评价。mate 默认 proposal-first——永不要求用户先想清楚，它先提，用户只负责把关。
- **影子模式**：初始两周只观察不打扰，每晚一条"今天注意到 X，若我是你的 mate 会做 Y。开吗？"能力靠用户批准长出来。
- **纠错即训练**：用户的"不对"比"对"信息量大，拒绝对应规则入库。
- **校准仪式**：每周 5 分钟 1:1，拿数据提议阈值调整，用户点头。不是配置面板，是周期性对齐。

### 2.7 关系模型（亲密轴不可翻牌）

- **权力轴可设计，亲密轴不可设计**。亲密是长时间可靠互动的涌现，不是功能。产品永远做不出"朋友"，只能做出"值得被当朋友的东西"。
- **向上靠长，向下靠翻**：同事 → 搭子 → 密友，每级由积累解锁（连续命中率、主动寻找频率、干预率、上下文记忆量），卡牌只能看不能翻；降级牌（"闭嘴几天只报告事故"）必须可翻。
- **伴侣牌永远在牌堆外**：伴侣意味着共同决策、利益绑定，超过产品能承诺的范围。放了就是画饼。
- 卖行为不卖感情：承诺"只说你不知道的、和你相关的"，不承诺"懂你"。

### 2.8 劳作可见性原则

- **双态**：静默态（默认，零打扰）+ 观赏态（用户主动打开，全剧场：干活可视化、动作流、日志流）。门开不开永远在用户手里。
- **履职回放 > 内容早报**：早报的真形态是监控回放——"昨晚 23:40 看到 X 判断为噪音滤掉；凌晨 2 点 CI 挂判断值得报告"。摘要可编造，回放可核查。这圆上了早报伪需求——伪的是内容早报，真的是履职回放。
- **禁假劳动**：进度条造假、"AI 正在思考"实为限流，识破一次信任清零。屏幕上的一切必须是真活。
- **禁谄媚**：顺从必须来自胜任，不来自表演。同事的可靠比宠物的讨好持久。
- **禁喂养机制**：用户不伺候它（无饥饿、无照顾需求）。它是被观赏的劳动者，不是被照顾的宠物。

## 3. 形态与设备分工

### 3.1 形态结论

- **IM/小程序**：主力触面。mate 是通讯录里的一位联系人，用户已经在 IM 里，不用去"打开"它。主动开口 = 发消息，天然符合打断协议。
- **Web 工作台**：摘要归档、记忆浏览、信任面板、权限设置。
- **桌宠**：在场层（不是产品本体）。桌面小生物状态显示 + 身体语言，解决"常在被感知"。
- **虚拟人物**：后期差异化再上，MVP 不做（做不好变玩具，且主动性靠判断质量不靠脸）。

### 3.2 设备分工（当前实际资源）

| 设备 | 角色 | 说明 |
|---|---|---|
| **安卓手机** | 眼睛 + 边缘脑 | NotificationListenerService 读全部通知/短信；PokeClaw 本地模型过滤+脱敏；原文不出设备；零 token 成本 |
| **iPhone** | 半聋的喇叭 | Shortcuts 桥送短信；Bark/PushDeer 收打断级推送 |
| **ESP32-S3** | 桌宠身体 + 看门狗 | 呼吸灯状态显示；物理隐私开关；环境传感器（温湿度、人感）；心跳失联告警 |
| **VPS** | 中枢脑 | 调度、队列、记忆、目标模型、打扰预算、执行沙箱、LiteLLM 网关、信任面板 |
| **微信小程序** | 家庭触面 | 提议交互、yes/no 循环、家庭数据入口、权限矩阵 UI |

安卓当主力传感器：唯一能合法读全部通知的平台。ESP32 不是玩具，是两件真东西：在场层（呼吸灯：绿=干活中，蓝=有摘要攒着，红=隐私模式，快闪=打断级）+ 物理隐私开关（硬件开关是最真的门，比 app 里点一下可信十倍）。

## 4. 架构：四层

```
┌─────────────────────────────────────────────────────────────────┐
│ 表达层  小程序(提议/摘要/回放)  Bark(打断)  ESP32(在场/看门狗)      │
├─────────────────────────────────────────────────────────────────┤
│ 中枢层  VPS: 调度(cron) + 队列 + 记忆(SQLite) + 目标模型           │
│          + 打扰预算器 + 执行沙箱 + 脱敏闸 + LiteLLM 网关            │
├─────────────────────────────────────────────────────────────────┤
│ 边缘层  安卓本地 PokeClaw: 分类、脱敏、重要度初判                  │
│         原文止于此层。只上送结构化信封                             │
├─────────────────────────────────────────────────────────────────┤
│ 感官层  安卓 NotificationListener / 小程序 / ESP32 传感器          │
│         / iPhone Shortcuts 桥                                     │
└─────────────────────────────────────────────────────────────────┘
```

### 4.1 各层职责

**感官层**：多适配器采集。Android 原生 / iOS Shortcuts / 浏览器扩展 / IM bot / webhook。每个源一个适配器，汇入本地 pipeline。

**边缘层**（安卓本地）：

- 本地小模型（PokeClaw，1-3B 量化）无限量跑：过滤、分类、脱敏、摘要。不烧 token。
- 原文不出设备。只有本地模型能看原文。
- 上送内容 schema 化，结构化信封：

```
{源, 时间, 类型, 实体hash, 重要度hint, 脱敏摘要≤N字}
```

- 敏感类事件本地模型自己判，不上送原文。非敏感类才允许 VPS 拉原文——默认不拉。

**中枢层**（VPS）：

- 调度（cron/APScheduler）：定时任务 + 事件驱动。
- 队列：提议队列、离线队列（VPS 挂时安卓本地照滤，提议进离线队列，恢复后 sync）。
- 记忆（SQLite）：目标模型、偏好、反馈历史、过滤阈值。导出即全部。
- 打扰预算器：出站计数器，超限降级摘要。
- 执行沙箱：动作注册表，每个动作声明档位 + 回滚方式。可逆 = 自动 undo 日志；不可逆 = 必须小程序内人工确认。
- 脱敏闸：所有出站调用强制过闸，命中敏感模式替换为占位符。
- LiteLLM 网关：小模型日常判断 → 大模型罕见深判 → fallback 链。

**表达层**：

- 小程序：提议、摘要、履职回放、yes/no 循环、信任面板。
- Bark/PushDeer：打断级推送。
- ESP32：呼吸灯状态 + 物理隐私开关 + 看门狗（心跳失联转琥珀色）。

### 4.2 数据流

```
感官 → 边缘(本地判,脱敏) → 信封 → 中枢(关联,预算) → 表达(分级通道) → 用户反应 → 记忆调阈值
```

### 4.3 五条铁律（架构级保证，不是承诺）

1. 原文不出边缘层
2. 出站必过脱敏闸（VPS → 外部模型 API 是唯一危险口，闸建在这）
3. 隐私一断全断（安卓断上送，VPS 收不到，ESP32 红）
4. 提议默认可逆（undo 日志）
5. 打扰有预算（表达层出站计数器）

## 5. 隐私模式

### 5.1 定义

降级到"纯传感器，零记忆，零云，零打扰"：

- **停记**：不写入 SQLite。RAM 处理，退出即清。
- **断网**：云调用全断。本地小模型照跑，出站 = 无。
- **静默**：不推送、不报告、不摘要。
- **不学**：该模式内事件不喂过滤层。用户不想让它知道的，它连"用户不想让它知道这件事"都不必知道。

一句话：**这段时间它假装不在，且这是架构保证的假装。** 不是"它看见了但承诺不说"（承诺会食言），是"它根本没记"。

### 5.2 隐私模式是常开模式的许可证

用户敢让 mate 24h 在场，全靠这面牌托底。没这面牌，没人敢让它盯着自己。三个硬保证：

1. **硬件开关**：ESP32 物理按钮一键翻。翻过去后无法远程再翻回来（否则被黑客叫醒，牌白翻）。
2. **零日志**：隐私期间无任何日志。连"进入了隐私模式"这条例外该不该记，用户自己选。
3. **出站审计**：事后可查——"隐私期间出站调用 0 次"。这行数字是信任锚点。

## 6. 敏感数据脱敏

### 6.1 验证码处理（硬规则）

1. **识别**：正则 + 本地模型双重。特征：短数字串 + "验证码/code" + 短时效语境。发件人白名单辅助。
2. **RAM 处理**：只在内存中分类和转发，不写入任何持久层。生命周期几秒。
3. **存储**：需要留存的存 OS keychain / Android Keystore。agent 记忆里没有。
4. **元数据只留壳**：agent 只记"某时某源来了验证码，已处理"。无码值。
5. **云端永不接触**：本地层直接短路，不进云模型、不上传、不打日志。管道上就是断的。

### 6.2 其余敏感类型分级

密码类、身份证号、银行卡号、地址——正则 + 本地模型识别，按等级决定"本地处理/脱敏存储/拒绝存储"。

### 6.3 脱敏闸

位于管线上游，所有出站调用强制过闸。任何要发给云模型的内容先过脱敏器，命中敏感模式直接替换为占位符 `[PHONE]` `[CODE]`。漏过一次就是信任崩塌事故。

本地优先到这里变成工程必然，不是卖点：短信/验证码这类源，法律和常识都不允许原文过云端。本地模型 + 本地存储不是选择，是**能接入这些数据源的前提**。反过来，敢接这些源，才拿得到别人拿不到的私有流。

## 7. 模型层设计

### 7.1 统一抽象

ModelClient 接口，两处实现：

- **边缘**：PokeClaw 本地推理（零 token 成本，无限量过滤）
- **中枢**：LiteLLM 网关（小模型日常判断 → 大模型罕见深判 → fallback 链，多 key 池）

多模型需求落成接口，不是配置文件。agent 本体零感知模型切换。

### 7.2 分级链

```
手机(小模型)全天候: 传感器+过滤+脱敏, 原文不出设备
  → VPS(中模型): 跨域关联、深判断
  → 云端大模型(罕见, 用户自己的 key): 复杂推理
```

每级只把"值得"的往上送。token 账单从"常燃"变"零星"。

### 7.3 经济学

- 过滤层无限量跑，成本零（本地推理）
- 云端只剩罕见升级，可选用户自己的 key
- 算力是用户的，电池是用户的。端侧推理直接掐断"每条消息都抽 token"的燃料管

## 8. 量化与信任面板

### 8.1 系统指标（打工人 KPI）

- **拦截率**：处理条数 / 总流入
- **干扰率**：误报 / 总推送（核心红线）
- **命中率**：用户响应 / 总推送（产品命根）
- **成本**：本地推理 vs 云调用 vs 用户时间（留意把成本推给用户是作弊）
- **响应时间**：事件发生到做出动作（含"决定不叫用户"）

### 8.2 关系指标（用户实际怎么对待它）

- **接受率**：提议被 yes 的比例。低 = 不懂用户；过高 = 太保守，只提用户本来会做的
- **反馈丰富度**：回"yes"还是"改成这样"——后者才是学到东西的信号
- **自主请求率**：没指派、自己开口的比例。测 mate 感本身
- **干预率**：用户主动叫停的频率。上升 = 信任在跌，警报
- **沉默取消**：不再用、不回复。最难测最致命

### 8.3 组合判读

| 状态 | 信号 | 解读 |
|---|---|---|
| 健康 | 命中率高 + 干预率低 | 真正有用的 mate |
| 奴性 | 接受率高 + 自主请求率低 | 工具伪装 |
| 骚扰 | 干扰率高 + 命中率低 | Clippy 复发 |
| 疏远 | 沉默取消上升 + 反馈变薄 | 用户在离开它 |

### 8.4 终极指标

**省时**：没有它用户盯这些要多久，现在多久。省时是终极指标，其余都服务它。且指标必须用户看得见——量化是信任货币，不是内部监控。它必须透明到自己能被解雇，能被解雇的 mate 才会被信任。

## 9. 家庭维度（小程序）

- 家庭 mate 比个人 mate 更强：管家是历史上真实职位，共享上下文天然 moat，信任强、粘性高、付费意愿现成。
- **家庭权限矩阵**：成员 = 各自边缘 + 共享中枢。事件标归属（我的/家庭的）。我的事走我的打扰预算，家庭事走家庭频道，可见性矩阵可查可改。这是平台做不了的权限模型。
- 隐私从"我的安心"变"全家刚需"（老人健康、孩子信息）。
- 信任面板全家共用："本月 mate 替咱家挡了 X 件事"——家庭场景里信任指标天然可分享可传播。

## 10. 风险清单

| 风险 | 对策 |
|---|---|
| Clippy 死因（烦） | 打扰预算 + 冷启动保守 + 反馈收缩 |
| 误报一次用户关推送 | 默认静默，提议档切入 |
| VPS 单点 | 离线队列 + 安卓本地照滤 + ESP32 看门狗 |
| 平台收紧权限（Android/iOS） | 多适配器架构，逐源可替换 |
| 微信订阅消息限制 | 分级通道：重要走 Bark，普通走小程序 |
| 假劳动识破 | 一切可视化内容是真活 |
| 模型能力不足 | 分级链：本地干本地活，深判断升云 |
| 信任摧毁快 | 保守是产品特性：宁可少说，不可错说 |

## 11. 构建路线图

1. **中枢 + 小程序**：VPS（LiteLLM + cron + SQLite）+ 小程序提议循环。家庭数据先动（账本异常/日程冲突/健康遗漏，先做一类）。楔子。
2. **表达层预算器**：小程序提案开始记账——信任面板第一批数据。
3. **安卓边缘**：NotificationListener + PokeClaw 本地过滤脱敏。先只观察不上送（两周影子期）。
4. **信封 schema + 脱敏闸**：结构化上送协议，出站闸门。
5. **ESP32 在场层**：状态灯 + 物理隐私开关 + 看门狗。
6. **iPhone 桥**：Shortcuts + Bark。低优先。

每步独立通电，先跑起来再补洞。

## 12. 商业模式

收用户钱，不收平台钱。订阅费买的是"它没拿平台的钱"。这是"唯一不把你当电池的 AI"的经济形式，也是平台 AI 结构性做不了的（平台必须把用户当电池，财报决定的）。

技术弱壁垒（过滤、关联谁都能做），位置中壁垒（第三方中立层，平台互相不信任时活下来，参考 Plaid/密码管理器），信任强壁垒（"只对我负责"的定位一旦成立，平台 AI 没法反向吃掉——用户不会相信平台的 AI 替自己省时间离开平台）。
