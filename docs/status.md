# Tuppy 项目状态

> 最后更新：2026-08-23

## 当前阶段

**代码层：v0.1 / v0.2 / v0.3(语音终端) 功能均已完成并通过测试，但真实使用未启动。**

**真实使用层（2026-08-23 VPS 直连核查）**：生产库仅 `entries=3 / todos=0 / proposals=1(expired,0 响应) / rule_log=0 / shadow=0`。系统长期处于「功能齐全但输入饥饿」状态——你自述「用得少、只收到一次通知」已被数据证实。

**结论：原判据（30 天跨度 + 接受率≥30%）前提不满足，08-30 不可能靠真实数据通过。不再被动等判据，改为「先制造使用」的主动节奏。**

**路线图（按 2026-08-23 规划重排）**：

```
现在   P0 烧录 v0.3 固件 + 语音录入跑通 + 养成每日录入（喂数据）
  ↓ 跑 2 周真实数据
08-30 P1 判据重定（行为判据：连续7天录入 + 周点击≥1）→ v0.2 真闭环
  ↓
      v0.3 硬件验收（烧录后实测）→ 再谈 v0.4
v0.4  安卓通知源 + 隐私牌   ← 今年不做（单用户阶段过度设计）
v0.5  静音牌/多成员/小程序  ← 今年不做
```

> 历史 tag：v0.1（闭环）、v0.2（功能完成，真实使用待 P0 启动后复测）。

## 已落地

| 组件 | 状态 | 位置 |
|---|---|---|
| 规则引擎（四模板 + 15 规则） | ✅ | engine.py + rules 表 |
| Web 终端（六页） | ✅ | app.py + templates/ |
| 条目编辑/删除 | ✅ | 录入页表格 |
| CSV/ICS 导入（去重） | ✅ | 端到端测试过 |
| Flask 登录页（替换 Basic Auth） | ✅ | /login，30 天 session |
| PWA | ✅ | manifest + 图标 |
| 规则页（浏览/导出/导入） | ✅ | /rules |
| 推送（自建 ntfy + 周报心跳） | ✅ | ntfy.oahubs.com |
| 页面更新按钮 | ✅ | trigger 文件 + root cron |
| 自动部署 | ✅ | cron 5 分钟 + push 后手动触发 |
| 测试 | ✅ 39→56 用例全过 | tests/ |
| MCP 语音终端（录入+查询双向） | ✅ | mcp_server.py + scripts/mcp_pipe.py |
| MQTT 设备状态发布 | ✅ | 班后 mosquitto_pub → 固件屏显 |
| 推送记录页（含 10 分钟响应率） | ✅ | /pushes，实时计算不另存 |

## 关键 URL

- Web：https://tuppy.oahubs.com（Flask 登录页）
- ntfy：https://ntfy.oahubs.com（topic `tuppy`，用户 `tuppy`）
- 仓库：https://github.com/tojoevan/tuppy
- 规则集：https://github.com/tojoevan/tuppy-rules

## 运行机制

```
cron 08:30 早班 → 扫规则 → 提议 ≤3 条 → 有提议则推手机（点击跳提议页）
cron 22:00 晚班 → 反馈降权 + 影子报告 + 备份 → 有待办则推手机
                  周日加周报心跳（存活证明）
```

推送链路：Tuppy POST → ntfy（127.0.0.1:2586）→ 手机 app。无事不推。iOS 唤醒走 ntfy.sh 中转（仅信号）。

MQTT 链路：Tuppy 班后 mosquitto_pub → 1883 → mosquitto → 1884(ws) → nginx /mqtt → wss → ESP32 固件（状态屏显）。

## 案例文档

定位/排障案例在 [docs/cases/](cases/)（按日期命名）。

## 规则生态

- 规则单一事实源：tuppy-rules 仓库 `rules.json`（格式标准 + 15 条规则 + 数据源地图）
- seed.sql 是生成物：`scripts/sync_rules.py` 从 rules.json 生成，不手改
- 生产库规则更新走规则页导入（seed 只在建库时播种）

## 判据（2026-08-23 重定）

> **原 14 天判据（30 天跨度数据 + 接受率≥30%）前提不成立**：2026-08-23 VPS 直连核查，生产库 `entries=3 / todos=0 / proposals=1(expired,0响应) / rule_log=0`，数据跨度仅 8 天、接受率 0%。单用户个人项目靠真实日常使用攒不满 30 天/30%，被动等 08-30 只会浪费时间。
>
> **改为行为判据（P0 启动使用后才验收）**：
> - [ ] 连续 7 天有录入（语音或 Web，任意来源）
> - [ ] 推送周点击 ≥1 次
> - [ ] 至少 1 条 proposal 被 kept/rejected（证明「对话」真发生）
>
> 满足即算 v0.1/v0.2 真闭环，不再卡 30 天/30%。验收时机 = P0（烧录语音固件 + 每日录入）跑满 2 周后。

## v0.2 复盘清单（冻结，等真实数据）

> 以下观察项依赖 rule_log / 影子报告真实触发，当前库全 0，**现在填无意义**。P0 跑 2 周后回头逐项填。

| # | 观察项 | 数据来源 | 结论 |
|---|---|---|---|
| 1 | 预算自动调节：配额降后是否回来响应 | rule_log + 影子报告 | ⬜ 待数据 |
| 2 | 降级静默进影子的感知 | 影子页打开数 | ⬜ 待数据 |
| 3 | 默认配额 2 是否合适 | 推送命中/静默比 | ⬜ 待数据 |
| 4 | 信任面板需不需要独立页 | 周报趋势够不够 | ⬜ 待数据 |
| 5 | 响应判定：次日响应是否误判沉默 | rule_log 时间戳 | ⬜ 待数据 |
| 6 | 配额恢复窗口：推送日 vs 自然日 | rule_log | ⬜ 待数据 |
| 7 | 待办到期提醒及时性/烦扰度 | ntfy 点击 + 反馈 | ⬜ 待数据 |

## v0.2 判据复盘清单（观察项，8/23 起可逐项核对）

来源：status.md §v0.2 观察项 + v0.3-design.md §2。

| # | 观察项 | 判定时机 | 数据来源 | 结论 |
|---|---|---|---|---|
| 1 | 预算自动调节：配额降后是否回来响应 | 首次降配额后 3 天 | rule_log + 影子报告 | ⬜ |
| 2 | 降级静默进影子的感知：能否发现"它有话没说" | 首次超预算后 | 影子页打开数 | ⬜ |
| 3 | 默认配额 2 是否合适 | 08-30 判据日 | 推送命中/静默比 | ⬜ |
| 4 | 信任面板需不需要独立页 | 08-30 | 周报趋势够不够用 | ⬜ |
| 5 | 响应判定：次日响应是否被误判沉默 | 首次连续 3 天判定后 | rule_log 时间戳 | ⬜ |
| 6 | 配额恢复窗口：有推送日 vs 自然日 | 首次恢复后 | rule_log | ⬜ |
| 7 | 待办到期提醒及时性/烦扰度 | 8/21 首次真触发后 | ntfy 点击 + 反馈 | ⬜ |

## 已知事项

- VPS 连 GitHub 不稳：fetch 12 秒超时跳过，scp fallback 可用；deploy 脚本已加固
- 生产库真实数据（2026-08-23 核查）：entries=3（信用卡/胰岛素/牛奶）、todos=0、proposals=1(expired)、push_log=8(08-16→08-22)、rule_log=0、shadow=0。输入饥饿，使用未启动。
- 种子规则 15 条（含缴费/车辆/宠物/证件/健康域），没数据的安静沉默
- ntfy 密码在 `/etc/ntfy/server.yml`（hash）+ `.env`（明文）两处，改密码需双同步 + 手机 app
- schema.sql 变更不会自动迁移，需手动处理
- 部署健康检查打 /login（根路径 302 是登录拦截，非故障）
- 08-23 文档刷新：status.md / README 对齐 v0.2 完成 + v0.3 进展；此前 9 个提交已 push 至 origin/main（不再本地裸奔）

## v0.2 进度（代码完成，真实使用待启动）

- ✅ 预算器设计定案（两级：规则级已有 + 通道级每日配额 2，自动调节，超预算静默进影子）
- ✅ 信任面板 v0.1：周报 8 周命中率趋势 + 规则页命中率列（样本<5 显示 —）
- ✅ 规则单条删除（带确认）
- ✅ entries 分页（每页 10 条，更早的/更新的）
- ✅ 数据导出（下载完整 SQLite，"数据是文件"兑现）
- ✅ 日志轮转（logrotate weekly ×4）
- ✅ 影子页节奏提示（第 7 天后看周报 top5）
- ✅ 推送记录页（/pushes，10 分钟响应率实时计算）
- ✅ 部署失败图标（更新按钮右侧 ⚠/✓）
- ⏳ **真实使用未启动**：rule_log=0 / shadow=0 / proposals 0 响应，上述机制从没被真实触发。等 P0（语音固件 + 每日录入）跑 2 周后复测。

## 下一步（由 P0 真实使用摩擦决定，不由路线图空想）

**P0（立即，回工作室第一件事）**
- 烧录 v0.3 固件（见 docs/v03-flash-checklist.md），语音录入「帮我记一下」跑通
- 养成每日录入习惯（Web 或语音至少 1 条/天），攒行为判据数据
- 推送从「汇报式」改「有用式」：早班带 1 条最该今天处理的（到期+高优先级）

**P1（08-30 前后）**
- 按新行为判据验收 v0.1/v0.2 闭环
- 回填 v0.2 复盘清单 7 项（已有真实数据后）

**P2（数据到位后）**
- 信任面板独立页、规则 provenance（命中率）、待办优先级强化
- 数据源地图扩充

**明确不做（今年）**
- v0.4 安卓通知源、v0.5 小程序/多成员/静音牌：单用户阶段过度设计，使用没跑通前不铺
